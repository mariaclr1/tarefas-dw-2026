version: "3"
services:
  db:
    image: mariadb:10.5
    container_name: "mariadb-dw"
    restart: always
    environment:
      - MARIADB_USER=root
      - MARIADB_ROOT_PASSWORD=123
    ports:
      - 3307:3306
